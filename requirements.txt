# Python >= 3.8 required (pathlib, f-strings)
#
# Used by:
#   scripts/build_hcm_level2.py — reads legacy .xls (xlrd) and .xlsx (openpyxl) files
#
# scripts/build_federal_composite_scores.py and scripts/import_closure_sheet.py
# use stdlib only and need no packages here.
#
# To recreate this environment:
#   pip install -r requirements.txt

xlrd==2.0.1
openpyxl==3.1.5
